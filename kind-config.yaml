# kind-config.yaml
# Launch using: 
# kind create cluster --config kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: demo # This sets the prefix for all node names
nodes:
- role: control-plane
- role: worker
- role: worker